# DEPRECATED / UNUSED.
#
# The site is no longer a pure static file server. It now runs as a small
# Node/Express app (see deploy/Dockerfile + docker-compose.yml) that
# server-renders the pages and exposes a runtime content admin at /admin.
#
# The HOST Caddy (/etc/caddy/Caddyfile on the VPS) still terminates TLS for
# tovaitech.in and forwards "/" to this container on port 8090. No in-container
# Caddy is used anymore; this file is kept only to avoid confusion.
